[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openemotion"
version = "0.1.0"
description = "Open-source facial emotion recognition using PyTorch and OpenCV"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
